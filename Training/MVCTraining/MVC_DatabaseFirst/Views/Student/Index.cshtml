@model IEnumerable<MVC_DatabaseFirst.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StudentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DOB)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Class)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DOB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Class)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentRollNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentRollNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentRollNumber })
        </td>
    </tr>
}

</table>
